# 프로젝트 개요

이 프로젝트는 여러 React 프로젝트에서 재사용할
사내 공통 UI 컴포넌트 라이브러리다.

## 반드시 참고할 자료

공통 컴포넌트를 분석하거나 설계하거나 구현할 때는
항상 아래 자료를 먼저 확인한다.

@docs/reference/UI Component 기초 분석자료_v1_20260721.html

세부 내용 확인이 필요한 경우 아래 원본 자료도 확인한다.

@docs/reference/ui-component-analysis.html

## 판단 우선순위

컴포넌트를 설계할 때는 다음 순서로 판단한다.

1. 현재 사용자가 명시한 요구사항
2. CLAUDE.md 규칙
3. 승인된 아키텍처 문서
4. UI 기초 분석자료
5. 기존 프로젝트 코드 규칙

자료와 실제 프로젝트 설정이 충돌하면
임의로 결정하지 말고 사용자에게 알려야 한다.

## 공통 컴포넌트 추출 규칙

- 화면 전체를 하나의 컴포넌트로 만들지 않는다.
- primitive, composite, layout, overlay, form, data-display로 분류한다.
- 업무 도메인에 종속된 값은 공통 컴포넌트 Props에 넣지 않는다.
- 기존 컴포넌트를 먼저 재사용하거나 확장할 수 있는지 검토한다.
- boolean Props가 지나치게 많아지면 composition 방식을 우선한다.
- 색상과 크기는 하드코딩하지 않고 디자인 토큰을 사용한다.
- 접근성, 키보드 조작, 포커스 처리를 반드시 검토한다.
- 로딩, 비활성, 오류, 빈 상태, 읽기 전용 상태를 필요한 경우 정의한다.

## 구현 전 필수 절차

공통 컴포넌트를 구현하기 전에 반드시 다음 순서로 진행한다.

1. UI 기초 분석자료에서 관련 패턴 확인
2. 현재 프로젝트의 기존 구현 확인
3. 공통화 대상인지 검토
4. 컴포넌트 책임 범위 제안
5. 공개 Props API 제안
6. 업무 전용 로직과 공통 UI 로직 분리
7. 접근성 요구사항 정의
8. 사용자 승인 후 구현 시작

## Superpowers 작업 방식

공통 컴포넌트 작업은 다음 흐름으로 진행한다.

1. brainstorming으로 구조와 요구사항 분석
2. 컴포넌트 분류와 아키텍처 제안
3. 필요한 질문을 한 번에 하나씩 확인
4. 사용자의 설계 승인 대기
5. writing-plans로 구현 계획 작성
6. 승인된 컴포넌트 묶음만 구현
7. typecheck, lint, test, build 실행